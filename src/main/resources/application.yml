spring:
  application:
    name: consulta-precios-api
  web:
    locale: en
    locale-resolver: fixed

springdoc:
  swagger-ui:
    url: /openapi/prices-api.yaml

management:
  endpoints:
    web:
      exposure:
        include: health
